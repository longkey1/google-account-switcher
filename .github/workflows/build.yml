name: Build Extension

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Create ZIP
        run: |
          zip -r extension.zip . -x "*.git*" ".github*" "README.md" ".gemini*"

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: google-account-switcher
          path: extension.zip
